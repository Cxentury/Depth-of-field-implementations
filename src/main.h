#include <raylib.h>
#include <array>
#include <iostream>
#include <iterator>
#include <direct.h> 

void draw_scene();
void DoFParameters(float *focusDistance,float *focusRange,float* maxBlurRad, Vector3 *position);