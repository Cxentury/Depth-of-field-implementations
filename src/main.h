#include <raylib.h>
#include <array>
#include <iostream>
#include <iterator>
#include <direct.h> 

void draw_scene();
void DoFParameters(Vector2 *lensParams,float* maxBlurRad,Vector2 *dilationParams, Vector3 *position);